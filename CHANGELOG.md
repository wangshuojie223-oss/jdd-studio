# 版本历史 · CHANGELOG

版本号规则：语义化递增（小修改 +0.0.1 / 新功能 +0.1.0 / 大改版 +1.0.0），每次更新同步 VERSION、package.json、pyproject.toml、README 顶部四处。

## v2.2.4（2026-09-04）
- 修复：剧多多模型改名导致生成失败——`Gpt-Image-2` 更名 `GPT Image 2`，`has_text` 子串匹配对连字符/空格敏感导致「模型不在下拉里」；模型匹配改为规范化比较（忽略大小写和空格/连字符/下划线），config 同步为新名，失败时报错附当前实际下拉选项
- 修复：管家菜单 4 登录撞 profile 锁——服务运行中（后台 Chrome 占用 chrome-profile）时登录弹不出窗口；现在检测到运行会先提示并确认关闭再登录，登录后提示重启
- 清理：全仓库 11 处过时「bun run xxx」文案，统一为管家菜单 / uv 命令

## v2.2.3（2026-08-20）
- 修复：海报模式「🧹 清空参考图」按钮点击无效且误弹「选择保存位置」窗口——目录快捷跳转的绑定误用样式类 `.qj` 作选择器，覆盖了清空按钮的 onclick；改用 `[data-jump]` 选择器根治

## v2.2.2（2026-08-13）
- 下载图片**沿用剧多多平台原始文件名**（OSS 对象名），不再改名；取不到时回退兜底命名，同批重名自动加后缀

## v2.2.1（2026-08-13）
- 选择窗口每组生成记录**默认折叠**，点 ▸ 展开；展开状态在自动刷新后保持

## v2.2.0（2026-08-13）—— 单页面多任务流水线
- **批间不排队**：A 角色投递完立刻在同页面投 B 角色，统一收集，taskId 精确归账互不串图
- 每个任务一条**独立进度卡**（角色名 + 组进度 + 百分比 + ETA），替代全局单进度条
- 终止逻辑适配共享页面：取消单个任务不再强关页面误伤其他任务
- 收集循环与后续投递并行；每任务独立超时/停滞/宽限判定
- 删除死代码（旧的 DOM diff 等图函数）

## v2.1.1（2026-08-13）
- 修复：悬停放大效果盖住图片勾选框（transform 层叠上下文问题，加 z-index 根治）
- 平台失败任务**诊断日志**：errorMsg/errorCode 落 server.log，界面错误显示原因摘要
- **全失败组宽限复验**：连续 3 轮轮询全败才盖棺（平台偶发先报失败后出图）
- 角色/海报两个提示词 skill 加入「拒绝 CG 感、3D 渲染感、塑料感」硬约束（含兜底模板）

## v2.1.0（2026-08-13）
- **每次启动自动清空参考图 + 剧本角色表**（与剧本同步归零，防止旧剧角色表污染新剧自动识别）
- 清空按钮挪到「角色参考图」标题行右侧

## v2.0.2（2026-08-13）
- 删除页头英文眉题；新场记板图形标三处统一：网页标题、浏览器 favicon、桌面/项目启动器图标

## v2.0.1（2026-08-13）
- 参考图区重构：**单张上传 / 候选表导入拆成两张独立入口卡**（各带说明）
- 删除参考图下的 🔍 重识别按钮；原生文件选择框换成统一按钮 + 文件名回显
- 视觉深化：宋体斜体工序号 + 发丝分隔线、卡片/图片悬停微交互

## v2.0.0（2026-08-13）—— 界面重塑「白色片场通告单」
- 白底 + 墨色主按钮 + 朱砂红强调色；宋体衬线标题/工序号 + 等宽数字字体
- **场记板工序导航**（签名元素）：吸顶、实时显示每步状态（已出词/N 组方案/N 张候选/待扩·已扩）、当前步骤高亮、点击直达卡片
- 模式切换升级为分段开关；方案卡勾选态可视化（未勾选淡化虚线）；画廊勾选红圈 + ✓ 角标
- 修复隐藏 bug：进度条区域因 CSS 优先级问题一直隐藏不掉

## v1.9.0（2026-08-13）
- **每组张数可选**：4 张（点 1 次）/ 6 张（平台选 3 张连点 2 次合并归组）；张数落库，重试自动沿用
- 角色/海报两模式各自记忆张数选择；第二次投递失败时降级保住已下发的 3 张

## v1.7.0 ~ v1.8.x（2026-08-12 ~ 08-13）
- 日常迭代：细节体验优化与稳定性修复

## v1.6.0（2026-08-12）—— 双模式彻底独立
- 角色/海报两模式的提示词模型下拉、提示词修改窗口**互不干扰**，各自记忆上次选择
- 默认提示词模型定为 gemini-3.6-flash（kimi-k3 处理 2 万字剧本超时）

## v1.5.0（2026-08-12）
- **飞书《角色候选表》xlsx 整单导入**：定版列优先取图、黄底格兜底、图片按锚点归位、缺图行跳过；预览确认后替换式导入

## v1.4.0（2026-08-12）
- **参考图视觉自动识别**：剧本角色表（roster）缓存 + 视觉模型自动填名带置信度；先传图后传剧本自动补识别；支持改名/重识别
- 踩坑固化：送视觉模型前必须缩图（768px JPEG），识别模型固定 flash 不受界面下拉影响

## v1.3.0（2026-08-12）
- 参考图面板操作打通：上传/删除/带参考图生成；带图生成慢，超时上调至 360s

## v1.2.0（2026-08-12）—— 功能二「剧本海报」
- 上传 .docx 剧本 → AI 通读 → 8 组海报提示词（6 亮调 2 暗调，平台安全区量化约束写进提示词）
- 复审闭环：送去扩图 → review_pending/ → 剪映 AI 扩展 → review_done/ 自动扫描 → 🛡 安全区叠加对照 → 采用入成品库
- 剪映驱动一期半自动（全自动 AX 点击待联调）

## v1.1.0（2026-08-11）
- **工作台管家.command 统一入口**：启动/关闭/打开页面/登录/打包/首次安装五合一；桌面转发图标
- 打包/迁移体系：Python zipfile 打包（保 UTF-8 中文名）、目标机首次安装向导

## v1.0.0（2026-08-10）—— 初版
- 功能一「角色图」全链路：角色描述 → N 组提示词 → 指定角色卡批量生成 → OSS 原图抓取 → 画廊挑选 → 平铺保存
- 无头后台运行、⏹ 一键终止、进度条 + ETA、skill 热读
